import { DealersPanel } from '@/components/contacts/DealersPanel';

export const metadata = {
  title: 'Dealers',
  description: 'Find official Suzuki dealers — filter by city and services, then get directions.',
};

export default function DealersPage() {
  return <DealersPanel />;
}
