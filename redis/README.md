# Redis

Конфигурация для контейнера `redis`.

| Параметр | Значение |
|----------|----------|
| Host (из хоста) | `localhost:6379` |
| Host (из backend) | `redis:6379` |

Файл конфигурации: `redis.conf`
