# Docker / CI: не спрашивать подтверждение при переустановке node_modules
confirm-modules-purge=false

# Windows: использовать hoisted режим чтобы избежать EPERM при создании симлинков
# (Developer Mode не нужен; файлы копируются напрямую в node_modules)
node-linker=hoisted
