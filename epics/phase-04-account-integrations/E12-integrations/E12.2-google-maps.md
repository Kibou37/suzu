# E12.2 — Google Maps

**Оценка:** ~8 ч | **Приоритет:** P0 | **Статус:** ✅

## Подзадачи

- [x] **E12.2.1** — Карта дилеров с маркерами (`/dealers`)
- [x] **E12.2.2** — «Get directions» deep link на карточке дилера
- [x] **E12.2.3** — Lazy load Maps JavaScript API, domain restriction через env key

## Реализация

- `packages/shared` — `dealers[]` с `city`, `services`, `location`
- `frontend/src/app/dealers/page.tsx` + `DealersPanel`
- Layout как на suzuki-motor.ru/dealers: карта на всю область, слева сверху теги:
  1. **Dealer list** — показать/скрыть список
  2. **City** — выбор города
  3. **Services** — Automobiles / Service & parts / Motorcycles
- `/contacts` → redirect на `/dealers`

## Настройка

1. Google Cloud: включить **Maps JavaScript API**
2. Ограничить ключ по HTTP referrer
3. `.env`: `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="..."`
