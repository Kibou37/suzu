# E19.5 — Контент: блог, FAQ, акции, статика

**Статус:** ✅ Admin CRUD + публичные FAQ/Blog/Promotions (главная) | **Оценка:** ~16 ч | **Приоритет:** P0 | **Зависимости:** E19.2 (+ E19.3 для картинок)

## Цель

Быстрый win: контент-менеджер правит тексты сайта в первую очередь здесь (до тяжёлого catalog UI).

## Подзадачи

### FAQ

- [x] **E19.5.1** — Admin CRUD FAQ + sort order + category (3ч) → `AdminFaqController/Service` + `FaqAdminPanel`
- [x] **E19.5.2** — Public `GET /api/faq` (E2.2.4) + страница `/faq` на сайте (2ч) → группировка по категориям, accordion

### Promotions

- [x] **E19.5.3** — CRUD promotions: title, description, image, dates, isActive (3ч) → `AdminPromotionsController/Service` + `PromotionsAdminPanel`
- [x] **E19.5.4** — Homepage banners CMS: `/admin/banners` (desktop+mobile upload, add/edit/delete) → главная `PromoSlider`

### Blog

- [x] **E19.5.5** — CRUD blog: slug (auto), title, excerpt, content, cover, publish (4ч) → `AdminBlogController/Service` + `BlogAdminPanel`
- [x] **E19.5.6** — Public `GET /api/blog`, `GET /api/blog/:slug` (2ч) → `/blog` список + `/blog/[slug]` детальная

### Static pages (minimal)

- [ ] **E19.5.7** — `SitePage` model: key (`about`, `contacts`), markdown blocks (2ч)

## UX

- Blog: preview button → opens public URL in new tab (pending — нет public route)
- [x] FAQ: numeric sort order (drag-and-drop — nice-to-have, не в v1)
- [x] Promotions: date range (`<input type="date">`), badge статуса

## DoD

- [x] FAQ/Blog/Promotion создаются, редактируются и удаляются в `/admin` (данные сразу в БД)
- [x] FAQ создан в admin → виден на публичной странице `/faq` (проверено e2e, `cache: 'no-store'`)
- [x] Статья blog draft → не на сайте (`isPublished: false` не отдаётся публичным API); publish → появляется на `/blog`
- [x] Баннеры главной редактируются в `/admin/banners` (desktop/mobile upload, порядок, add/remove) → видны на `/`

## Реализовано (backend)

- `backend/src/admin/{admin-faq,admin-blog,admin-promotions}.{controller,service}.ts`
- `backend/src/admin/dto/{faq,blog-post,promotion}.dto.ts`
- `backend/src/admin/admin.util.ts` — `uniqueBlogSlug`
- `backend/src/content/{faq,blog}.controller.ts` + `content.module.ts` — публичные read-only эндпоинты (`GET /api/faq`, `GET /api/blog`, `GET /api/blog/:slug`, только `isPublished`)

## Реализовано (frontend)

- Admin: `frontend/src/components/admin/{FaqAdminPanel,BlogAdminPanel,PromotionsAdminPanel}.tsx`, `frontend/src/app/admin/(panel)/{faq,blog,promotions}/page.tsx`, `frontend/src/lib/admin-api.ts`
- Публичный сайт: `frontend/src/app/faq/page.tsx` (accordion по категориям), `frontend/src/app/blog/page.tsx` + `frontend/src/app/blog/[slug]/page.tsx`, `frontend/src/lib/{faq,blog}.ts`
- Ссылка «FAQ» добавлена в футер
