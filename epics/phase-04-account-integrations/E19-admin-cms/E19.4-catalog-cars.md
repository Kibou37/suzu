# E19.4 — Каталог: модели Suzuki

**Оценка:** ~24 ч | **Приоритет:** P0 | **Зависимости:** E19.2, E19.3

## Цель

Редактирование каталога без правки `seed.ts` — цены, описания, наличие, акции, базовые медиа.

## Фазы внутри эпика

### E19.4.A — MVP catalog (~12 ч)

- [x] **E19.4.1** — List cars: table (name, slug, price, condition, offer badge, actions) (3ч) — `CarsAdminPanel.tsx`
- [x] **E19.4.2** — Edit car: name, slug, trim, price, year, mileage, bodyType, fuel, transmission, description, `isOffer`, `condition` (4ч)
- [x] **E19.4.3** — Gallery: ordered image URLs (textarea, одна ссылка на строку) (3ч) — без визуального media picker (см. E19.3)
- [ ] **E19.4.4** — Publish toggle / `isPublished` flag → скрыть с сайта (2ч) — нет в схеме, отложено

### E19.4.B — Configurator data (~12 ч, v1.1)

- [ ] **E19.4.5** — Variants CRUD (base price per variant) (3ч)
- [ ] **E19.4.6** — Colors: body/interior swatches, hex, price delta (4ч)
- [ ] **E19.4.7** — Options: groups, exclusive flags, prices (4ч)
- [ ] **E19.4.8** — Import/export JSON (backup конфигуратора) (1ч)

> 360° viewer assets в v1 — **upload paths + JSON**, без visual 360-editor.

## DoD (MVP E19.4.A)

- [x] Изменение цены Swift в admin → новая цена на `/catalog/swift` (Redis cache инвалидируется через `AdminCarsService.invalidateCache`)
- [x] Новая акция `isOffer` → бейдж на карточке (форма + список в `CarsAdminPanel`)
- [ ] Unpublish → модель не в публичном списке (нет `isPublished`, отложено до появления реальной необходимости)

Smoke: `node scripts/e19-admin-smoke.mjs` — создание/удаление модели, проверка видимости на `/api/cars` без редеплоя ✅.

## Риски

| Риск | Митигация |
|------|-----------|
| Сложность Car↔Variant↔Option | MVP только Car; variants позже |
| Конфигуратор demo-data в TS | Постепенный перенос options в БД |
