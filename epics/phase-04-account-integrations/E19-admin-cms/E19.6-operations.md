# E19.6 — Операции: брони, quotes, слоты

**Оценка:** ~14 ч | **Приоритет:** P1 | **Зависимости:** E19.1, E19.2

## Цель

Менеджер дилера видит заявки и календарь без Postman и SQL.

## Подзадачи

### Bookings

- [x] **E19.6.1** — List bookings: filter by type/status (3ч) — `BookingsAdminPanel.tsx`; поиск по телефону не реализован (мало записей в MVP, сортировки по дате достаточно)
- [x] **E19.6.2** — Change status (PENDING / CONFIRMED / COMPLETED / CANCELLED) прямо из таблицы (2ч) — отдельная detail-страница не потребовалась

### Quote requests

- [x] **E19.6.3** — Inbox quote requests: model, summary, contact, dealer (3ч) — `QuotesAdminPanel.tsx`
- [x] **E19.6.4** — Смена статуса (PENDING / CONTACTED / CLOSED) (1ч) — отдельного поля internal note нет, есть `notes` клиента

### Service slots

- [x] **E19.6.5** — UI списка слотов с фильтром по датам/типу + форма создания (3ч) — `ServiceSlotsAdminPanel.tsx`; полноценный calendar-виджет не потребовался для MVP
- [x] **E19.6.6** — Block slot / adjust maxBookings инлайн в таблице (2ч)

## DoD

- [x] Менеджер блокирует слот в UI → слот недоступен на форме ТД (флаг `isBlocked` учитывается в `getAvailableSlots`)
- [x] Quote из конфигуратора виден в inbox с summary/snapshot-описанием
- [x] Статус брони меняется → отражается в ЛК пользователя (тот же `Booking.status`)

## Переиспользование

- `backend/src/bookings/booking-slots-admin.controller.ts` — мигрирован с `AdminApiKeyGuard`/`X-Admin-Key` на `JwtAuthGuard + AdminRolesGuard` (E19.2.5 закрыт), путь оставлен `bookings/admin/slots`
- Новые `AdminBookingsController/Service` и `AdminQuotesController/Service` в `backend/src/admin/*`, зарегистрированы в `AdminModule`

Smoke: `node scripts/e19-admin-smoke.mjs` покрывает bookings/quotes/slots CRUD и проверяет, что старый `X-Admin-Key` больше не принимается.
